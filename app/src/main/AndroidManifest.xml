<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.isec.supraindustries.kitchendiy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KitchenDIY">
        <activity android:name=".PrintReceita"></activity>
        <activity android:name=".Informacoes" />
        <activity android:name=".LerReceita" />
        <activity android:name=".VerReceitas" />
        <activity android:name=".DetailsActivity" />
        <activity android:name=".EscolherIngredientes" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>